#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "exercises.h"
#include "new_user_questionnaire.h"
#include "routine.h"
#include "workouts.h"

void main() {
    // exercise* all_exercises = create_all_exercises();
    // questionnaire user_questionnaire = create_questionaire();
    // exercise* available_exercises = create_available_exercises(all_exercises, user_questionnaire);
    // exercise* used_exercises = create_used_exercises(available_exercises, user_questionaire);
    // workout* workouts = create_workouts(used_exercises, user_questionaire);
    // print_routine(workout);

    // Tests for workout_session.c


}